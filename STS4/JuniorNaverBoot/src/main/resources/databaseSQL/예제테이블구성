멤버                                                              member
아이디 문자열                                                id     varchar(10)
이름 문자열                                                   name   varchar(20)
나이 수치자료형                                             age    number(3)


create table member(

	id varchar(10) primary key not null,
	name varchar(20),
	age number(3)



);